Agglom
------

Some simple code for looking at agglomeration processes using scala

Fairly simple code could be compiled and run directly, but is designed to be compiled and run using "sbt"

Once sbt is installed, just run from this directory and type "run" at the sbt prompt


